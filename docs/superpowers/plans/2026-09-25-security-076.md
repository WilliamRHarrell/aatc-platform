# Security 076 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the open security items from the 2026-09-24 audit: aatc_submissions gets a migration home with its live shape and four policies pinned, the last anon write policy goes, admins can add a pinup entry by hand, the verify blocks derive their policy lists from the catalog, and a verify proves a signed-in non-owner reads nobody else's rows.

**Architecture:** Migration 076 (delivered, not applied; APPLY AFTER PR 1 DEPLOYS because it drops the sponsor anon insert) pins aatc_submissions with `create table if not exists` matching the live catalog Ryan read on 2026-09-25 (FKs to auth.users, defaults), re-creates its four policies verbatim `to authenticated`, adds "admins insert pinup entries", drops "Anyone can submit sponsor application". verify_076 pins the shape and policies, asserts no anon-reachable write policy from the catalog, and runs the non-owner read check as the RLS harness user with fixtures. verify_075 F and verify_074 F4 lose their hardcoded lists. `/admin/pinup` gains an Add Entry card (`AddPinupEntry`, `validateAdminPinupEntry` tested).

**Tech Stack:** Supabase RLS + plpgsql, Next.js 16, vitest. Approved by Ryan 2026-09-25 ("076: accepted").

**Decisions:** D1 the admin policies on aatc_submissions keep their inline profiles check (verbatim) rather than is_admin(); HANDOFF records that the two are equivalent in result and that is_admin() is the safer form. D2 the by-hand pinup entry picks confirmed/waitlist explicitly and skips the capacity lock and the emails on purpose. D3 verify_076 E uses fixtures for the tables that may be empty live so "0 rows" is a refusal, not an empty table.

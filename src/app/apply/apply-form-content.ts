/** Registry-backed copy shared by the artist and vendor application forms (key applyForms). */
export interface ApplyFormContent {
  /** Veteran upload field label and help text. */
  veteranDocLabel: string
  veteranDocHelp: string
}
